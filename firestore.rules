rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All content operations use authenticated server routes and the Admin SDK.
    // Client access is denied by default; Admin SDK access is controlled by IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
