rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Uploads are validated and performed by authenticated server routes.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
